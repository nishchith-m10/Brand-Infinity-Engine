# Indexes Inventory (Snapshot)

**Generated:** January 13, 2026

This document lists the current indexes present on the production database (snapshot taken during investigation) and recommends a small set of safe, non-breaking indexes to add if desired. No changes were applied.

---

## Existing Indexes (by table)

- brand_knowledge_base:
  - `brand_knowledge_base_brand_id_idx`
  - `brand_knowledge_base_deleted_at_idx` (partial: deleted_at IS NOT NULL)
  - `brand_knowledge_base_is_active_idx`
  - `brand_knowledge_base_knowledge_base_id_idx`
  - `brand_knowledge_base_pkey` (id)
  - `idx_brand_kb_active`
  - `idx_brand_kb_brand_id`
  - `idx_brand_kb_deleted_at` (partial)
  - `idx_brand_kb_knowledge_base_id`

- budget_reservations:
  - `budget_reservations_pkey` (id)
  - `idx_budget_reservations_campaign_status` (campaign_id, status) WHERE status = 'reserved'
  - `idx_budget_reservations_created` (created_at) WHERE status = 'reserved'
  - `idx_budget_reservations_request` (request_id)

- campaigns:
  - `campaigns_status_idx` (status)
  - `campaigns_user_id_idx` / `idx_campaigns_user` / `idx_campaigns_user_id` variants
  - `idx_campaigns_brand_id`
  - `idx_campaigns_user_status`
  - `idx_campaigns_deleted_at` (partial)
  - `idx_campaigns_created_at`
  - multiple others (locked_by indexes, pkey)

- content_requests:
  - `content_requests_status_idx`
  - `content_requests_campaign_id_idx`
  - `content_requests_brand_id_status_idx`
  - `content_requests_created_at_idx`
  - `content_requests_created_by_idx`
  - `idx_content_requests_status_created` (status, created_at)
  - `idx_content_requests_campaign_status`
  - `idx_content_requests_brand_status`
  - `idx_content_requests_created_by`

- cost_ledger:
  - `idx_cost_campaign`, `idx_cost_idempotency`, `idx_cost_ledger_campaign_id`, `idx_cost_ledger_created_at`, etc.

- provider_metadata:
  - `idx_provider_metadata_job_id` (external_job_id)
  - `idx_provider_metadata_provider` (provider_name)
  - `idx_provider_metadata_task_id` (request_task_id)

- request_events:
  - `idx_request_events_request_timeline` / `idx_request_events_request_id`
  - `idx_request_events_created_at`
  - `idx_request_events_type`

- request_tasks:
  - `idx_request_tasks_request_status`
  - `idx_request_tasks_request_id`
  - `idx_request_tasks_agent_role`
  - `idx_request_tasks_sequence`
  - `idx_request_tasks_status`

- scripts:
  - `idx_scripts_approval_status`
  - `idx_scripts_brief_approval`
  - `idx_scripts_created_at`
  - `idx_scripts_variant_tag`

- videos:
  - `idx_videos_approval_status`
  - `idx_videos_campaign_id`
  - `idx_videos_created_at`
  - `idx_videos_deleted_at` (partial)
  - `idx_videos_script_approval` (script_id, approval_status)
  - `idx_videos_script_id`
  - `idx_videos_total_cost`, `idx_videos_quality_score`


## Observations

- Most of the indexes we intended to add are already present but sometimes with slightly different names (e.g., `campaigns_user_id_idx` vs `idx_campaigns_user_id`). The schema uses both `created_by` and `user_id` in different tables; the scripts & videos tables use `approval_status`, not `status` consistently.

- `budget_reservations` table and its indexes were successfully created and are present.

- Many tables already have `created_at` and `deleted_at` columns; several partial indexes already exist for `deleted_at IS NOT NULL` filtering.

## Recommended Next Steps (Read-only, no changes applied)

- If you want to align naming conventions, consider a follow-up migration that renames indexes for consistency (low value; prefer leaving existing names intact to avoid lock contention).

- The remaining performance improvements are mostly already in place; if you want, I can prepare a safe, conditional migration that adds any missing indexes conforming to current column names and naming patterns — I will not apply it without your explicit approval.

---

*This report was generated by an automated inspection of the database schema and index catalog. No database schema changes were performed.*
